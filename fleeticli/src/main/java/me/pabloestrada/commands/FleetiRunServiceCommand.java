package me.pabloestrada.commands;

import picocli.CommandLine;

import java.io.File;

public class FleetiRunServiceCommand implements Runnable {

/*
    @CommandLine.Option(
            names = { "-r" },
            description = "Type of Fleeti subcommand ()")
    private boolean rebuild;
*/

    @Override
    public void run() {
//        final String executionPath = System.getProperty("user.dir");
//        final File projectFile = new File(executionPath);
//        final String projectName = projectFile.getName();
//        if (new File(executionPath + "/service/target/"))
    }
}
